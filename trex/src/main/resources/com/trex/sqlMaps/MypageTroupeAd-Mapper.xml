<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="MypageTroupeAd-Mapper">

	<update id="insertTroupeAd">
		insert into ad (ad_code, ad_num , title, cont, writer)
		values(#{ad_code} ,ad_seq.nextval,#{title},#{cont},#{writer})
	</update>

	<select id="selectTroupeAdByNum" >
		select *
		from
		ad
		where ad_num=#{ad_num}
	</select>
	
	<update id="increaseViewCnt">
		update ad set viewcnt=viewcnt+1
		where
		ad_num=#{ad_num}
	</update>

	<sql id="search">
		<if test="searchType == 't'.toString()">
			and title like '%'||#{keyword}||'%'
		</if>
		<if test="searchType == 'c'.toString()">
			and content like '%'||#{keyword}||'%'
		</if>
		<if test="searchType == 'w'.toString()">
			and writer like '%'||#{keyword}||'%'
		</if>
		<if test="searchType == 'tc'.toString()">
			and (
			title like '%'||#{keyword}||'%'
			or
			content like '%'||#{keyword}||'%'
			)
		</if>
		<if test="searchType == 'cw'.toString()">
			and (
			content like '%'||#{keyword}||'%'
			or
			writer like '%'||#{keyword}||'%'
			)
		</if>
		<if test="searchType == 'tcw'.toString()">
			and (
			title like '%'||#{keyword}||'%'
			or
			content like '%'||#{keyword}||'%'
			or
			writer like '%'||#{keyword}||'%'
			)
		</if>
	</sql>

	<select id="selectSearchTroupeAdList" >
		<![CDATA[
			select * 
			from ad
			where ad_num>0
		]]>
		<include refid="search"></include>
		order by ad_num desc
	</select>
	<select id="selectSearchTroupeAdCount" resultType="int">
		<![CDATA[
			select count(*)
			from ad
			where ad_num>0
		]]>
		<include refid="search"></include>
	</select>


	<select id="selectMemberBoardAll" resultType="qnaboard">
		select *
		from qna
		order by qna_num desc,regDate desc
	</select>

</mapper>